// Định nghĩa tiền tố cho các đường dẫn liên quan đến admin
const ADMIN_PREFIX = "admin";

// Xuất đối tượng chứa tiền tố admin để sử dụng ở các nơi khác
module.exports = {
  adminPrefix: ADMIN_PREFIX, // Trả về giá trị của biến ADMIN_PREFIX
};
